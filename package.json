{
  "name": "leo-web",
  "version": "0.0.1",
  "description": "node实战训练",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm run clean && babel src -d target --watch",
    "compile": "npm run clean && babel src -d target",
    "clean": "rm -rf target",
    "dev": "export NODE_ENV=dev && export DEBUG='my:*' && node target/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/leo-work/leo-web.git"
  },
  "author": "leo <leo16241022@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/leo-work/leo-web/issues"
  },
  "homepage": "https://github.com/leo-work/leo-web#readme",
  "dependencies": {
    "body-parser": "^1.15.1",
    "debug": "^2.2.0",
    "express": "^4.13.4",
    "mongoose": "^4.4.20",
    "serve-static": "^1.11.1"
  }
}
